package org.randoom.setlx.setlXMusic.musicSystem.storages;

/**
 * These are types, that a {@link MusicStorage} can have.
 */
public enum StorageTypes {
    PATTERN_STORAGE, // iff MusicStorage<Pattern>
    RHYTHM_STORAGE, // ...
    CHORD_PROGRESSION_STORAGE,
    UNSUPPORTED_TYPE
}
